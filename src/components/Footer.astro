---
import { siteConfig } from "../config/site";
import { getDatasetStats } from "../lib/communities";

const stats = getDatasetStats();
---

<footer class="mt-auto border-t-[3px] border-black bg-white text-black">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 lg:py-16">
    <div class="grid grid-cols-1 gap-8 sm:grid-cols-2 md:grid-cols-4 lg:grid-cols-5">
      <!-- Brand & Mission Column -->
      <div class="lg:col-span-2 space-y-4">
        <a href="/" class="flex items-center gap-2.5">
          <div class="flex h-9 w-9 items-center justify-center rounded-xl bg-[#FEF08A] text-black border-2 border-black shadow-[2px_2px_0px_0px_#000] font-black text-sm">
            ??
          </div>
          <span class="text-base font-black text-black">{siteConfig.name}</span>
        </a>
        <p class="text-xs font-medium leading-relaxed text-slate-700 max-w-sm">
          Discover public WhatsApp, Telegram, and Discord communities sharing job openings, hiring alerts, career opportunities, and remote work across the USA, UK, Canada, and Australia.
        </p>

        <div class="rounded-xl border-2 border-black bg-[#FEF9C3] p-3.5 text-xs text-black font-bold space-y-1.5 shadow-[2px_2px_0px_0px_#000]">
          <div class="flex items-center justify-between">
            <span>Active Job Communities:</span>
            <strong class="text-black font-black text-sm">{stats.totalPublished}</strong>
          </div>
          <div class="flex items-center justify-between">
            <span>Target Markets:</span>
            <span class="text-black font-black">USA, UK, Canada, Australia</span>
          </div>
          <div class="flex items-center justify-between">
            <span>Platforms:</span>
            <span class="text-black font-black">Telegram, WhatsApp, Discord</span>
          </div>
        </div>
      </div>

      <!-- Countries Column -->
      <div>
        <h4 class="text-xs font-black uppercase tracking-wider text-black bg-[#BAE6FD] px-2 py-0.5 rounded border border-black shadow-[1px_1px_0px_0px_#000] inline-block mb-3">
          Target Countries
        </h4>
        <ul class="space-y-2 text-xs font-bold">
          {siteConfig.footerNav.countries.map((item) => (
            <li>
              <a href={item.href} class="hover:underline text-black">
                {item.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Job Types Column -->
      <div>
        <h4 class="text-xs font-black uppercase tracking-wider text-black bg-[#FEF08A] px-2 py-0.5 rounded border border-black shadow-[1px_1px_0px_0px_#000] inline-block mb-3">
          Job Categories
        </h4>
        <ul class="space-y-2 text-xs font-bold">
          {siteConfig.footerNav.jobTypes.map((item) => (
            <li>
              <a href={item.href} class="hover:underline text-black">
                {item.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Trust & Legal Column -->
      <div>
        <h4 class="text-xs font-black uppercase tracking-wider text-black bg-[#A7F3D0] px-2 py-0.5 rounded border border-black shadow-[1px_1px_0px_0px_#000] inline-block mb-3">
          Trust & Safety
        </h4>
        <ul class="space-y-2 text-xs font-bold">
          {siteConfig.footerNav.trustAndLegal.map((item) => (
            <li>
              <a href={item.href} class="hover:underline text-black">
                {item.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <!-- Bottom Disclaimer & Trademark -->
    <div class="mt-12 pt-8 border-t-2 border-black/10 text-xs text-slate-600 font-medium space-y-3">
      <p class="leading-relaxed">
        <strong>Disclaimer:</strong> {siteConfig.name} is an independent public directory. Not affiliated with WhatsApp, Telegram, Discord, Meta, or employers represented in external communities. The website is an informational directory, not an employer or recruitment agency, and does not guarantee employment. Never pay money to get a job, and independently verify employers before sharing sensitive personal details.
      </p>
      <div class="flex flex-wrap items-center justify-between gap-4 pt-2 font-bold text-black">
        <span>© {new Date().getFullYear()} {siteConfig.name}. All rights reserved.</span>
        <div class="flex gap-4">
          <a href="/safety" class="hover:underline">Job Scam Guide</a>
          <a href="/how-we-verify" class="hover:underline">How We Verify</a>
          <a href="/editorial-policy" class="hover:underline">Editorial Policy</a>
          <a href="/disclaimer" class="hover:underline">Disclaimer</a>
          <a href="/privacy" class="hover:underline">Privacy</a>
          <a href="/terms" class="hover:underline">Terms</a>
        </div>
      </div>
    </div>
  </div>
</footer>
